<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.error_notification %>
  <%= f.input :description %>
  <%= f.association :ingredient, prompt: "ingredient"%>
  <%= f.button :submit %>
<% end %>
